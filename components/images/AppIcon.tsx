import React, { ReactElement } from "react";

// Bootstrap
import Image from "react-bootstrap/Image";

const AppIcon = (): ReactElement => {
    return <Image src="/app_icon.png" roundedCircle height="75px" />;
};

export default AppIcon;
